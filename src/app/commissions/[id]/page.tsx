import { notFound } from "next/navigation";
import Link from "next/link";
import { Check, Clock, PencilSimple } from "@phosphor-icons/react/dist/ssr";
import { artists, services } from "@/data/mock-data";
import { StatusPill } from "@/components/status-pill";
export default async function ServicePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const service = services.find((item) => item.id === id); if (!service) notFound(); const artist = artists.find((item) => item.id === service.artistId)!; return <div className="mx-auto grid max-w-6xl gap-10 px-4 py-12 sm:px-6 lg:grid-cols-[1fr_380px]"><div><Link href={`/artists/${artist.id}`} className="text-sm font-bold text-plum">← @{artist.username}</Link><h1 className="mt-5 font-[family-name:var(--font-display)] text-5xl leading-none sm:text-7xl">{service.title}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-black/60">{service.description}</p><div className="mt-10"><h2 className="text-xl font-bold">What’s included</h2><ul className="mt-4 grid gap-3 sm:grid-cols-2">{service.includes.map((item) => <li key={item} className="flex items-center gap-3 rounded-2xl bg-white p-4"><span className="grid size-7 place-items-center rounded-full bg-emerald-50 text-emerald-700"><Check size={15} weight="bold" /></span>{item}</li>)}</ul></div></div><aside className="h-fit rounded-4xl border border-black/[0.07] bg-white p-6 shadow-card"><StatusPill status={service.availability} /><p className="mt-5 text-sm text-black/45">Starting at</p><p className="text-4xl font-bold">${service.startingPrice}</p><div className="mt-6 space-y-3 border-y border-black/[0.07] py-5 text-sm"><p className="flex items-center justify-between"><span className="flex items-center gap-2 text-black/50"><Clock size={18} />Turnaround</span><strong>{service.turnaround}</strong></p><p className="flex items-center justify-between"><span className="flex items-center gap-2 text-black/50"><PencilSimple size={18} />Revisions</span><strong>{service.revisions} included</strong></p></div><button className="mt-6 w-full rounded-full bg-plum py-3.5 text-sm font-bold text-white">Request commission</button><p className="mt-3 text-center text-xs text-black/40">No payment is taken until the artist accepts.</p></aside></div> }
